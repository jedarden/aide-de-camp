All four children are set up. The split is complete.

```
SPLIT_COMPLETE: Created 4 children, parent converted to umbrella
```

```
Children: adc-3kt, adc-17z, adc-5hi, adc-4cd
```

**Chain:** adc-3kt → adc-17z → adc-5hi → adc-4cd → adc-4yw (umbrella)

1. **adc-3kt** — Add stale badge HTML/CSS element (prominent amber/red "STALE" badge in card header)
2. **adc-17z** — Add `setInterval` to refresh displayed times client-side every 60s (so "5m ago" advances)
3. **adc-5hi** — Replace binary CSS classes with gradual opacity fade proportional to age
4. **adc-4cd** — Backend: use `surfaced_at`/`acked_at` from results as staleness anchor instead of `topic.last_active`
