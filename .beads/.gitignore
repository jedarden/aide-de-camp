# SQLite database files
*.db
*.db-shm
*.db-wal

# Lock files
*.lock

# Temporary files
*.tmp
*.temp

# Journals
*.journal

# Worker transcripts are runtime artifacts and can contain command output,
# environment details, or credentials. Keep them local; the durable bead state
# lives in checkpoint/ and events.jsonl.
traces/
