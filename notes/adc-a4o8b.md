# whisper-stt Deployment Logs (adc-a4o8b)

## Summary

Retrieved and saved whisper-stt deployment logs to `logs/whisper-stt-raw.jsonl`.

## Deployment Details

**Cluster:** ardenone-cluster (read-only via kubectl-proxy)
**Namespace:** whisper-stt
**Pods:**
- `whisper-openai-68966786fb-jsb5d` (53 days uptime) — OpenAI-compatible whisper endpoint
- `whisper-stt-847fd8d7b9-v2rs5` (25 days uptime) — Asterisk-specific WebVTT service

## Logs Retrieved

| Pod | Records | Notes |
|-----|---------|-------|
| whisper-openai | 96,185 | uvicorn health check logs (HTTP GET /health) |
| whisper-stt | 0 | Silent service — no stdout/stderr logs |

## Time Coverage

- **Range:** 2026-07-10T13:39:33 to 2026-08-06T23:14:34
- **Duration:** 27 days (within requested 30-day retention)
- **Retention:** Full available logs from pod uptime (53 days for whisper-openai, 25 days for whisper-stt)

## Output File

**File:** `logs/whisper-stt-raw.jsonl`
- **Size:** 40 MB
- **Records:** 96,185
- **Format:** JSONL with fields:
  - `timestamp` — ISO 8601 with timezone
  - `pod_name` — Kubernetes pod name
  - `namespace` — whisper-stt
  - `app` — whisper-openai or whisper-stt
  - `raw_log` — Original kubectl log line
  - `log_content` — Log message without timestamp
  - `http_method` — HTTP method (if present)
  - `http_path` — Request path (if present)
  - `http_status` — HTTP status code (if present)
  - `http_message` — HTTP message (if present)

## Log Content

The whisper-openai logs are primarily uvicorn health check probes:
```
INFO: 10.42.2.1:43574 - "GET /health HTTP/1.1" 200 OK
```

These are generated by:
- Liveness probes (periodSeconds: 30)
- Readiness probes (periodSeconds: 10)
- Startup probes (initialDelaySeconds: 10, periodSeconds: 10)

The whisper-stt pod produces no logs — likely configured to log silently or to a different output.

## Collection Method

```bash
kubectl --server=http://traefik-ardenone-cluster:8001 logs \
  -n whisper-stt \
  whisper-openai-68966786fb-jsb5d \
  --timestamps=true
```

Logs were fetched via read-only kubectl-proxy over Tailscale, then parsed and converted to JSONL using Python.
