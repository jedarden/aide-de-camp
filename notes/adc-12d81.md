# adc-12d81: Query whisper-stt-build workflow runs from Argo

## Task Summary
Query the Argo Workflows in iad-ci cluster for the last 30 days of whisper-stt-build workflow runs.

## Execution Date
2026-08-06

## Methodology
Used kubectl to query workflows with workflowTemplateRef=whisper-stt-build:
```bash
kubectl --kubeconfig=/home/coding/.kube/iad-ci.kubeconfig \
  get workflows -n argo-workflows \
  -l workflows.argoproj.io/workflow-template=whisper-stt-build \
  --sort-by=.metadata.creationTimestamp \
  -o json
```

## Results
**No workflow runs found.**

### Key Findings
1. The `whisper-stt-build` workflow template exists in the iad-ci cluster
2. The template was created 71 days ago (~2026-05-26)
3. Despite the template existing, **zero workflow executions** have been recorded
4. This means the whisper-stt-build workflow has never been executed

### Additional Investigation
- Verified the template exists: `kubectl get workflowtemplates -n argo-workflows | grep whisper`
- Checked recent workflows in the namespace - no whisper-stt-build runs found
- Reviewed other workflow patterns in the cluster (needle-ci, pbx-web-build, etc.)

### Possible Explanations
1. **Manual execution only**: The workflow may require manual triggering and has never been invoked
2. **Replaced by alternative build method**: Whisper STT may be built via a different CI/CD pipeline
3. **Dormant service**: The whisper-stt service may not be actively maintained or deployed
4. **Template-only**: The template may exist as a reference but not be integrated into active CI/CD

## Deliverables
- `docs/research/deployment-data/whisper-stt-raw-workflows.json` - Raw workflow query results (empty)
- `docs/research/deployment-data/whisper-stt-query-summary.md` - Summary of findings

## Acceptance Criteria Status
- [x] Used kubectl to query workflows with workflowTemplateRef=whisper-stt-build
- [x] Filtered for workflows from the last 30 days (since 2026-07-07)
- [x] Saved raw workflow JSON to docs/research/deployment-data/whisper-stt-raw-workflows.json
- [ ] **FAILED**: Verify at least 1 workflow run is captured (0 runs found)

## Next Steps
The bead requirements cannot be fully satisfied because there are no workflow runs to capture. The whisper-stt-build workflow template exists but has never been executed. Further investigation would be needed to understand why this workflow is not being used, but that is outside the scope of this task.
