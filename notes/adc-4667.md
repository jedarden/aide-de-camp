# ADC-4667: Telegram Send Locations - Complete Inventory

## Task: Find all Telegram send locations in adc codebase

**Date:** 2026-07-02  
**Status:** Complete

---

## Summary

The adc codebase contains **10 locations** related to Telegram message sending, with **only 1 active send implementation** and several stub/unimplemented methods due to architectural constraints.

---

## Active Telegram Send Implementation

### 1. Primary Send Method
**File:** `src/telegram/fallback.py`  
**Lines:** 46-93 (`send_message` method)

**Implementation Details:**
- **Method Signature:** `async def send_message(self, chat_id: int | str, message: str, parse_mode: str = "HTML") -> bool`
- **HTTP Request:** `POST {self.bridge_url}/send`
- **Timeout:** 10.0 seconds
- **Payload:** `{"chat_id": ..., "text": ..., "parse_mode": "HTML"}`

**Error Handling:**
- `except httpx.RequestError as e` - Network/connection errors
- `except Exception as e` - Unexpected errors
- Non-200 status codes handled via error message
- All error paths call `_handle_send_failure(error_msg)`

**Current Logging:**
- ✅ **Success:** `logger.info(f"Sent Telegram message to chat {chat_id}")` (line 78)
- ✅ **First Failure:** WARNING with detailed error context (lines 215-219)
- ✅ **Repeated Failures:** DEBUG level only (rate-limited, lines 223-226)

**Rate Limiting:**
- Failure log cooldown: 300 seconds (5 minutes)
- Prevents log spam while maintaining visibility

---

## Active Wrapper Methods

### 2. Result Sender
**File:** `src/telegram/fallback.py`  
**Lines:** 95-106 (`send_result` method)

- Delegates to `send_message`
- Inherits all error handling and logging from `send_message`

### 3. Bridge Health Check
**File:** `src/telegram/fallback.py`  
**Lines:** 166-179 (`check_bridge_available` method)

- HTTP Request: `GET {self.bridge_url}/health`
- Timeout: 5.0 seconds
- Error handling: Generic exception handler
- Sets reachability state

---

## Stub/Unimplemented Send Methods

### 4. Exception Send (Not Implemented)
**File:** `src/telegram/fallback.py`  
**Lines:** 108-125

**Status:** Logs warning, returns False  
**Reason:** Missing session→telegram_chat_id mapping  
**Logging:** `logger.warning(...)` about pull-based architecture

### 5. Workload Summary Send (Not Implemented)
**File:** `src/telegram/fallback.py`  
**Lines:** 127-144

**Status:** Logs warning, returns False  
**Reason:** Missing session→telegram_chat_id mapping  
**Logging:** `logger.warning(...)` about pull-based architecture

### 6. Bead Watcher Telegram Send (Not Implemented)
**File:** `src/watcher/daemon.py`  
**Lines:** 215-240 (`_send_to_telegram` method)

**Status:** Logs warning only  
**Reason:** Missing session→telegram_chat_id mapping  
**Logging:** `logger.warning(...)` about pull-based architecture

### 7. Surface Registration (No-Op Stub)
**File:** `src/telegram/fallback.py`  
**Lines:** 146-164

**Status:** Returns True (API compatibility)  
**Reason:** telegram-claude-bridge doesn't support surface registration  
**Logging:** `logger.warning(...)` about no-op stub

---

## Supporting Infrastructure

### 8. Startup Bridge Check
**File:** `src/main.py`  
**Lines:** 149-161

**Logging:**
- ✅ Success: `logger.info(f"Telegram bridge reachable at ...")`
- ✅ Unreachable: Warning with bridge URL
- ✅ Check failure: `logger.warning(f"Failed to check Telegram bridge reachability: {e}")`

### 9. Bridge Status API Endpoint
**File:** `src/main.py`  
**Lines:** 1471-1483 (`api_v1_telegram_bridge_status`)

**Logging:**
- ✅ `logger.error(f"Error getting Telegram bridge status: {e}")` on exception
- Returns 500 with error message

### 10. Failure Handler (Rate-Limited)
**File:** `src/telegram/fallback.py`  
**Lines:** 197-226 (`_handle_send_failure` method)

**Strategy:**
- Sets `_is_reachable = False`
- Increments `_failure_count`
- First failure → WARNING
- Repeated within 5min → DEBUG

---

## Error Handling Assessment

### ✅ Properly Handled Locations
1. **`send_message` (fallback.py:46-93)** - Comprehensive try/except with specific handlers
2. **Bridge health check** - Generic but adequate exception handling
3. **API status endpoint** - Exception handling with user-friendly error response

### ⚠️ Stub/Warning-Only Locations
1. **`send_exception`** - Logs architecture mismatch warning
2. **`send_workload_summary`** - Logs architecture mismatch warning
3. **`_send_to_telegram` (daemon.py)** - Logs architecture mismatch warning

### 🔍 Missing Features
- No retry logic for failed sends
- No circuit breaker pattern
- No session→telegram_chat_id mapping (blocks several send methods)

---

## Configuration

**Environment Variable:**
- `ADC_TELEGRAM_BRIDGE_URL` (default: `http://telegram-claude-bridge:8000`)

**Timeouts:**
- Send timeout: 10.0 seconds
- Health check timeout: 5.0 seconds
- Failure log cooldown: 300 seconds

**Test Coverage:**
- `tests/test_telegram_fallback.py` - Unit tests for send logic
- `tests/test_telegram_bridge_status.py` - Status tracking tests

---

## Conclusion

The adc codebase has **exactly one working Telegram send implementation** (`send_message`) with **comprehensive error handling** and **rate-limited WARNING-level logging**. The rate-limiting prevents log spam while maintaining visibility.

Several other send methods remain unimplemented due to architectural constraints (telegram-claude-bridge uses pull-based architecture, not push-based delivery).

**All active send locations have appropriate WARNING-level logging:**
- First failure → WARNING with context
- Subsequent failures → DEBUG (rate-limited)

No additional WARNING logs are needed - the current implementation follows best practices for failure visibility without log spam.
