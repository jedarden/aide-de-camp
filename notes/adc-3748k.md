# Task adc-3748k: Create Basic JSONL File Reader

## Status: Already Implemented

The required `src/parse_log.py` module already exists with full implementation of the `load_jsonl()` function.

## Verification

All task requirements are met:

### 1. Module Created ✅
- File exists: `src/parse_log.py`
- Function signature: `def load_jsonl(file_path: str) -> Iterator[Dict]:`

### 2. Basic Parsing ✅
- Uses `json.loads()` per line (line 51)
- Yields individual parsed dict objects (line 52)
- Generator function with `Iterator[Dict]` return type

### 3. File Handling ✅
- Proper `with open()` context manager (line 42)
- Error handling for missing files with `FileNotFoundError` (lines 36-37)
- UTF-8 encoding specified
- File path validation (lines 39-40)

### 4. Line Tracking ✅
- Line numbers tracked via `enumerate(f, 1)` (line 43)
- Line number included in warning messages (line 54)

### 5. Error Handling ✅
- Logs warnings for unparseable lines instead of raising exceptions (lines 54-55)
- Continues processing after parse errors
- Empty lines are skipped (lines 47-48)

### 6. No Field Extraction ✅
- Module handles only basic JSONL loading
- No field extraction or transformation logic (as required)

## Implementation Details

The function:
- Accepts file path as string
- Returns a generator that yields dict objects
- Handles missing files appropriately
- Provides detailed error reporting with line numbers
- Continues processing on individual line parse errors
- Skips empty lines gracefully

The implementation follows Python best practices with proper type hints, logging, and error handling.
