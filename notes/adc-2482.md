# adc-2482 — escalate/handler.py + watcher/daemon.py: `br` → `bf` migration

## Outcome

**Already complete in committed code; verified, no source changes required.**

The bead description (which targets `src/escalate/handler.py` and
`src/watcher/daemon.py` invoking the deprecated `br` binary) describes a state
that no longer exists in the working tree. The `br` → `bf` migration it asks for
was already performed in commit **`24b32a9`** ("feat(telegram): expose
last_check_time + chat_id in bridge status (adc-4hvx)"), which flipped every
occurrence in `handler.py`:

- executable: `"br"` → `"bf"` (both `create_bead` and `_create_bead_with_type`)
- log/exception messages: `br create failed` → `bf create failed`,
  `br CLI not found` → `bf CLI not found`
- docstrings/comments: `br CLI` → `bf CLI` throughout

`src/watcher/daemon.py:37` already reads `Integration with the bf CLI
(bead-forge)`, and `src/escalate/__init__.py`, `src/main.py:575`, and
`src/fetch/orchestrator.py:581` all already say `bf`.

## Verification performed

1. **Grep across all of `src/`** for `"br"` / `br CLI` / `br create` / standalone
   `br` token / `subprocess.*br` → **zero matches**. Every bead-creation
   subprocess call uses `bf`.

2. **`bf create --help`** (bead-forge 0.3.0) confirms the flags the code builds
   are supported identically: `--title`, `--type` (default `task`),
   `--description`, `--label` — all present with the same semantics `br` had (as
   expected, since `br` is a shim that delegates to `bf`).

3. **End-to-end test of the real code path** (not just a manual `bf` call):
   instantiated `EscalateHandler` in an isolated `bf init`-ed temp workspace and
   called its actual `create_bead()` method with a test `EscalateRequest`. It
   built the full `bf create --title … --type task --description … --label k=v …`
   command, ran it, and **returned bead id `bf-53z`**; `_extract_bead_id` parsed
   the output correctly. The temp workspace was removed afterward, so the real
   aide-de-camp `.beads/` was not polluted.

4. **Tests** (`tests/test_kubectl_delete_pod_integration.py`,
   `tests/test_bead_watcher.py`) already align with `bf` — they mock
   `asyncio.create_subprocess_exec` without asserting any executable name and
   reference `_create_bead` / `bf` in comments. No stale `br` assertions.

## Acceptance criteria

- [x] No source file under `src/` invokes the `br` binary; all bead-creation
      subprocess calls use `bf`.
- [x] Comments/docstrings referencing "br CLI" updated to "bf CLI" /
      "bf (bead-forge)".
- [x] `_create_bead` confirmed to create and return a bead id end-to-end against
      a live `bf` (0.3.0) workspace — not static-only.

## Notes

- No version bump / tag cut: this commit is documentation only (no functional
  change to adc). The actual migration shipped with `24b32a9`.
- The unrelated unstaged changes in the working tree (`src/watcher/daemon.py`
  Telegram-fallback refactor, `src/telegram/fallback.py`,
  `tests/test_telegram_fallback.py`, etc.) belong to a separate workstream and
  were intentionally left untouched.
